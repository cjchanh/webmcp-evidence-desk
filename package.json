{
  "name": "webmcp-evidence-desk",
  "version": "0.1.0",
  "description": "Evidence Desk - AI receipts for claims that matter. WebMCP tools over a signed synthetic vendor handoff packet.",
  "type": "module",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build:manifest": "node scripts/build-manifest.mts",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "1.9.7"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.18"
  }
}
