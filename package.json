{
  "name": "webmcp-evidence-desk",
  "version": "0.1.0",
  "description": "Evidence Desk — a person verifies what an agent did before acting. WebMCP tools over a signed synthetic vendor handoff packet.",
  "type": "module",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build:manifest": "node scripts/build-manifest.mts",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "1.9.7"
  },
  "devDependencies": {
    "happy-dom": "^20.11.6",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
